import org.apache.spark
import org.apache.spark.type.Stru

object Kafka1 {
  def main(args: Array[String]): Unit = {
    val stockSchema =StructType(List(StructField))

    val KafkaSourceDF=spark.re

  }

}
